// Use "0" to jump at the start of the line and "$" to jump at the end of the line.
//
// Need to remove the extra letters at the start and end of the line to fix the errors.

const game1 = () => {
G  return `You need to delete the extra letters at the start and end of the line to fix the errors.`; G
}
